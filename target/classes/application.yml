server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_db?serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  mail:
    host: smtp.qq.com
    port: 587
    username: 2690835582@qq.com
    password: usfgirjwvzsodhed  # QQ邮箱授权码
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  main:
    banner-mode: "off"  # ?? Spring Boot ??
  application:
    name: demo

logging:
  level:
    org.springframework: WARN
    com.zaxxer.hikari: WARN
    com.baomidou.mybatisplus: WARN
    org.mybatis: WARN
    com.example.demo: INFO  # ??????????

jwt:
  secret: myVeryLongSecretKey123456789012345678901234567890123456789012345678901234567890  # 至少64字符的密钥
  expiration: 86400    # Token过期时间，单位秒（24小时）
  clock-skew: 300      # 时钟偏差容忍度，单位秒（5分钟）

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    default-type-handler-package: com.baomidou.mybatisplus.extension.handlers
  mapper-locations: classpath*:/mapper/*Mapper.xml

# 文件上传配置
spring.servlet.multipart:
  enabled: true
  max-file-size: 10MB
  max-request-size: 50MB
